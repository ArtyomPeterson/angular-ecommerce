import { MasuraShopValidators } from './masura-shop-validators';

describe('MasuraShopValidators', () => {
  it('should create an instance', () => {
    expect(new MasuraShopValidators()).toBeTruthy();
  });
});
